import { StyleSheet } from 'react-native';

export default StyleSheet.create(
  {
    page: {
      display: 'flex',
      width: '100%',
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center'
    },
    button: {
      borderWidth: 1,
      borderColor: 'black',
      borderStyle: 'solid',
      padding: 10,
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      margin: 10
    }
  }
);
