import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  page: {
    display: 'flex',
    width: '100%',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
